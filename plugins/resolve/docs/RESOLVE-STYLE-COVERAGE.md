# Resolve Fusion Text+ style coverage

Generated by `plugins/resolve/aksharo_core_app/fusion/style_map.py` (`scripts/generate_style_coverage.py`) from `packages/caption-styles/styles/*.json` and `packages/fonts/pack/fonts.json` — never hand-edited. Re-run the script after either changes.

**19 supported, 6 approximate, 5 unsupported** of 30 styles.

Every style reaches picture regardless of this table: C08's caption builder (`aksharo_core_app/captions.py`) uses a pre-rendered alpha overlay fallback whenever a style isn't Text+-exact, so *unsupported*/*approximate* here means "not a native, re-timeable Fusion Text+ clip in the Resolve timeline", never "missing from the export."

`assRenderable`/`parityScore` (A18a) are a *different* gate — Skia vs. libass parity for the `.ass` sidecar/web-player path — shown here only for cross-reference; the Supported/Approximate/Unsupported column is this module's own Text+ capability classification, driven by `classification_rules.json`.

| Style | Category | Status | Reason | Font (Resolve) | assRenderable | parityScore |
|---|---|---|---|---|---|---|
| Arcade Pixel (`arcade-pixel`) | gaming | Supported | — | Roboto Mono 700 | False | 0.9031 |
| Bold Drop (`bold-drop`) | bold | Supported | — | Anton 400 | True | 0.9485 |
| Box Block (`box-block`) | clean | Supported | — | Inter 700 | False | 0.8986 |
| Bubble Soft (`bubble-soft`) | playful | Approximate | word highlight is a per-word background box; Text+ has one whole-text Background, so the macro approximates it with the HighlightStart/HighlightEnd character-range colour input instead of a real drawn box. | Poppins 700 | False | 0.9004 |
| Caption Card (`caption-card`) | clean | Approximate | word highlight is a per-word background box; Text+ has one whole-text Background, so the macro approximates it with the HighlightStart/HighlightEnd character-range colour input instead of a real drawn box. | Inter 600 | False | 0.8629 |
| Duo Tone (`duo-tone`) | playful | Supported | — | Poppins 700 | True | 0.9521 |
| Glitch Shift (`glitch-shift`) | gaming | Supported | — | Anton 400 | True | 0.9550 |
| Gradient Sweep (`gradient-sweep`) | playful | Supported | — | Poppins 800 | False | 0.8906 |
| Highlight Marker (`highlight-marker`) | education | Approximate | word highlight is a per-word background box; Text+ has one whole-text Background, so the macro approximates it with the HighlightStart/HighlightEnd character-range colour input instead of a real drawn box. | Inter 700 | True | 0.9652 |
| Hype Bold (`hype-bold`) | bold | Supported | — | Anton 400 | False | 0.9292 |
| Impact Shout (`impact-shout`) | bold | Approximate | one word is shown on screen at a time; the macro approximates this by keyframing the Text field to swap per word at HighlightStart/HighlightEnd boundaries rather than natively paging words like the web renderer. | Anton 400 | False | 0.8276 |
| Karaoke Fill (`karaoke-fill`) | karaoke | Unsupported | word highlight is a progressive left-to-right fill wipe; Text+ has no per-glyph masked reveal control to build it from. | Poppins 700 | False | 0.8482 |
| Liquid Glass (`liquid-glass`) | minimal | Unsupported | the background is a translucent/blurred (glass) fill; Text+'s Background control is a flat opaque colour with no backdrop blur. | Inter 600 | False | 0.8671 |
| Minimal Lower Third (`minimal-lower-third`) | minimal | Supported | — | Inter 500 | False | 0.9256 |
| Neon Glow (`neon-glow`) | bold | Unsupported | word highlight is a glow; Text+ has no glow control and the macro does not stack a separate Glow tool per word. | Montserrat 800 | False | 0.9097 |
| News Ticker (`news-ticker`) | education | Supported | — | Inter 700 | False | 0.9038 |
| Outline Only (`outline-only`) | clean | Supported | — | Montserrat 800 | True | 0.9565 |
| Podcast Duo (`podcast-duo`) | podcast | Unsupported | word highlight is an underline; Text+ has no per-character text-decoration control. | Montserrat 600 | False | 0.9145 |
| Prism Split (`prism-split`) | bold | Supported | — | Montserrat 900 | True | 0.9489 |
| Punch Pop (`punch-pop`) | bold | Supported | — | Inter 900 | True | 0.9430 |
| Quote Frame (`quote-frame`) | cinematic | Approximate | the style asks for italic but no italic OFL variant of the mapped font is bundled; the macro falls back to Text+'s synthetic Italic (skew) toggle on the upright weight-matched font. | Playfair Display 500 | False | 0.7693 |
| Soft Serif (`soft-serif`) | cinematic | Supported | — | Playfair Display 600 | True | 0.9438 |
| Spotlight Word (`spotlight-word`) | karaoke | Supported | — | Poppins 800 | True | 0.9413 |
| Stroke Heavy (`stroke-heavy`) | bold | Supported | — | Montserrat 900 | True | 0.9522 |
| Subtitle Classic (`subtitle-classic`) | minimal | Supported | — | Inter 500 | True | 0.9599 |
| Tape Retro (`tape-retro`) | retro | Supported | — | Roboto Mono 700 | False | 0.9211 |
| Typewriter Mono (`typewriter-mono`) | retro | Unsupported | word highlight is an underline; Text+ has no per-character text-decoration control. | Roboto Mono 500 | False | 0.8531 |
| Vertical Clean (`vertical-clean`) | clean | Supported | — | Inter 600 | True | 0.9626 |
| Whisper Thin (`whisper-thin`) | minimal | Supported | — | Inter 300 | True | 0.9807 |
| Word Pop (`word-pop`) | playful | Approximate | one word is shown on screen at a time; the macro approximates this by keyframing the Text field to swap per word at HighlightStart/HighlightEnd boundaries rather than natively paging words like the web renderer. | Poppins 800 | False | 0.9230 |

## Known limitations (not scored per-style above)

- **Non-Latin script fallback fonts are not selectable per character.** Every style carries `typography.fallbacks` (e.g. Noto Sans Devanagari/Tamil) for scripts the primary font lacks; the macro's single `Font` input cannot switch fonts mid-string, so a Hinglish/Indic caption authored through the macro renders in the primary font only (missing glyphs show as `.notdef` boxes) — the alpha-overlay fallback remains the correct path for multi-script captions until a future macro revision adds per-run font inputs.
- **Fusion-side verification is pending.** No DaVinci Resolve install exists on this build host (A00-04 spike). This module's classification is derived from the StyleDoc fields and Text+'s documented capabilities, not from loading the macro in Resolve. Real-Fusion verification is tracked in `docs/GATE-C-CHECKLIST.md`.
