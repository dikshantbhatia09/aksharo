# After Effects styled-text-layer coverage (C05b, generated)

Generated by `pnpm --filter @montaj/ae-cep generate:style-coverage` from `src/styles/ae-style-map.ts` (classification rules mirrored from C08b's `plugins/resolve/aksharo_core_app/fusion/classification_rules.json`, see `src/styles/classification-rules.ts` — same rule set C06b's Premiere MOGRT mapping mirrors) against `packages/caption-styles/styles/*.json`. Do not edit by hand.

**19 supported, 6 approximate, 5 unsupported** of 30 styles.

Every style reaches picture regardless of this table: `src/apply/applyCaptions.ts` falls back to the pre-rendered alpha overlay path (A20) whenever a style isn't text-layer-exact, so _unsupported_/_approximate_ here means "not a native, re-timeable styled text layer in the After Effects timeline", never "missing from the export."

| Style                 | Status      | Reasons                                                                                                                                                                                                      |
| --------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `arcade-pixel`        | Supported   | —                                                                                                                                                                                                            |
| `bold-drop`           | Supported   | —                                                                                                                                                                                                            |
| `box-block`           | Supported   | —                                                                                                                                                                                                            |
| `bubble-soft`         | Approximate | word highlight is a per-word background box; this minimal panel has no per-word geometry, so it approximates this with the layer's own fill colour applied to the whole cue instead of a real drawn box.     |
| `caption-card`        | Approximate | word highlight is a per-word background box; this minimal panel has no per-word geometry, so it approximates this with the layer's own fill colour applied to the whole cue instead of a real drawn box.     |
| `duo-tone`            | Supported   | —                                                                                                                                                                                                            |
| `glitch-shift`        | Supported   | —                                                                                                                                                                                                            |
| `gradient-sweep`      | Supported   | —                                                                                                                                                                                                            |
| `highlight-marker`    | Approximate | word highlight is a per-word background box; this minimal panel has no per-word geometry, so it approximates this with the layer's own fill colour applied to the whole cue instead of a real drawn box.     |
| `hype-bold`           | Supported   | —                                                                                                                                                                                                            |
| `impact-shout`        | Approximate | one word is shown on screen at a time; this WP has no per-word text swapping (no full TimelineAdapter, per the brief), so it approximates by showing the whole segment's text for its full duration instead. |
| `karaoke-fill`        | Unsupported | word highlight is a progressive left-to-right fill wipe; a plain AE text layer has no per-glyph masked reveal control to build it from.                                                                      |
| `liquid-glass`        | Unsupported | the background is a translucent/blurred (glass) fill; a plain text layer has no backdrop-blur box to approximate it with.                                                                                    |
| `minimal-lower-third` | Supported   | —                                                                                                                                                                                                            |
| `neon-glow`           | Unsupported | word highlight is a glow; a styled text layer has no glow control and this WP does not stack a separate Glow effect per word.                                                                                |
| `news-ticker`         | Supported   | —                                                                                                                                                                                                            |
| `outline-only`        | Supported   | —                                                                                                                                                                                                            |
| `podcast-duo`         | Unsupported | word highlight is an underline; a plain AE text layer's TextDocument has no per-character underline control.                                                                                                 |
| `prism-split`         | Supported   | —                                                                                                                                                                                                            |
| `punch-pop`           | Supported   | —                                                                                                                                                                                                            |
| `quote-frame`         | Approximate | the style asks for italic but no italic OFL variant of the mapped font is bundled; the text layer falls back to a synthetic/faux italic on the upright weight-matched font.                                  |
| `soft-serif`          | Supported   | —                                                                                                                                                                                                            |
| `spotlight-word`      | Supported   | —                                                                                                                                                                                                            |
| `stroke-heavy`        | Supported   | —                                                                                                                                                                                                            |
| `subtitle-classic`    | Supported   | —                                                                                                                                                                                                            |
| `tape-retro`          | Supported   | —                                                                                                                                                                                                            |
| `typewriter-mono`     | Unsupported | word highlight is an underline; a plain AE text layer's TextDocument has no per-character underline control.                                                                                                 |
| `vertical-clean`      | Supported   | —                                                                                                                                                                                                            |
| `whisper-thin`        | Supported   | —                                                                                                                                                                                                            |
| `word-pop`            | Approximate | one word is shown on screen at a time; this WP has no per-word text swapping (no full TimelineAdapter, per the brief), so it approximates by showing the whole segment's text for its full duration instead. |
