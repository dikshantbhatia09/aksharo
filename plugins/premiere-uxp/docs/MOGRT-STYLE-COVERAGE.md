# MOGRT style coverage (C06b, generated)

Generated by `pnpm --filter @montaj/premiere-uxp generate:style-coverage` from `src/styles/mogrt-map.ts` (classification rules mirrored from C08b's `plugins/resolve/aksharo_core_app/fusion/classification_rules.json`, see `src/styles/classification-rules.ts`) against `packages/caption-styles/styles/*.json`. Do not edit by hand.

**19 supported, 6 approximate, 5 unsupported** of 30 styles.

Every style reaches picture regardless of this table: C06's apply-modes flow uses the pre-rendered alpha overlay path (A20) whenever a style isn't MOGRT-exact, so _unsupported_/_approximate_ here means "not a native, re-timeable MOGRT clip in the Premiere timeline", never "missing from the export."

| Style                 | Status      | Reasons                                                                                                                                                                                                                 |
| --------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `arcade-pixel`        | Supported   | —                                                                                                                                                                                                                       |
| `bold-drop`           | Supported   | —                                                                                                                                                                                                                       |
| `box-block`           | Supported   | —                                                                                                                                                                                                                       |
| `bubble-soft`         | Approximate | word highlight is a per-word background box; the MOGRT has no per-word geometry, so it approximates this with the HighlightColour/HighlightStart/HighlightEnd character-range colour sweep instead of a real drawn box. |
| `caption-card`        | Approximate | word highlight is a per-word background box; the MOGRT has no per-word geometry, so it approximates this with the HighlightColour/HighlightStart/HighlightEnd character-range colour sweep instead of a real drawn box. |
| `duo-tone`            | Supported   | —                                                                                                                                                                                                                       |
| `glitch-shift`        | Supported   | —                                                                                                                                                                                                                       |
| `gradient-sweep`      | Supported   | —                                                                                                                                                                                                                       |
| `highlight-marker`    | Approximate | word highlight is a per-word background box; the MOGRT has no per-word geometry, so it approximates this with the HighlightColour/HighlightStart/HighlightEnd character-range colour sweep instead of a real drawn box. |
| `hype-bold`           | Supported   | —                                                                                                                                                                                                                       |
| `impact-shout`        | Approximate | one word is shown on screen at a time; the MOGRT approximates this by keyframing Text to swap per word at HighlightStart/HighlightEnd boundaries rather than natively paging words like the web renderer.               |
| `karaoke-fill`        | Unsupported | word highlight is a progressive left-to-right fill wipe; the animator range selector has no per-glyph masked reveal control to build it from.                                                                           |
| `liquid-glass`        | Unsupported | the background is a translucent/blurred (glass) fill; BoxFill/BoxOpacity give a flat colour with no backdrop blur.                                                                                                      |
| `minimal-lower-third` | Supported   | —                                                                                                                                                                                                                       |
| `neon-glow`           | Unsupported | word highlight is a glow; the MOGRT has no glow control and does not stack a separate Glow effect per word.                                                                                                             |
| `news-ticker`         | Supported   | —                                                                                                                                                                                                                       |
| `outline-only`        | Supported   | —                                                                                                                                                                                                                       |
| `podcast-duo`         | Unsupported | word highlight is an underline; the animator range selector has no per-character underline control.                                                                                                                     |
| `prism-split`         | Supported   | —                                                                                                                                                                                                                       |
| `punch-pop`           | Supported   | —                                                                                                                                                                                                                       |
| `quote-frame`         | Approximate | the style asks for italic but no italic OFL variant of the mapped font is bundled; the MOGRT falls back to a synthetic/faux italic on the upright weight-matched font.                                                  |
| `soft-serif`          | Supported   | —                                                                                                                                                                                                                       |
| `spotlight-word`      | Supported   | —                                                                                                                                                                                                                       |
| `stroke-heavy`        | Supported   | —                                                                                                                                                                                                                       |
| `subtitle-classic`    | Supported   | —                                                                                                                                                                                                                       |
| `tape-retro`          | Supported   | —                                                                                                                                                                                                                       |
| `typewriter-mono`     | Unsupported | word highlight is an underline; the animator range selector has no per-character underline control.                                                                                                                     |
| `vertical-clean`      | Supported   | —                                                                                                                                                                                                                       |
| `whisper-thin`        | Supported   | —                                                                                                                                                                                                                       |
| `word-pop`            | Approximate | one word is shown on screen at a time; the MOGRT approximates this by keyframing Text to swap per word at HighlightStart/HighlightEnd boundaries rather than natively paging words like the web renderer.               |
