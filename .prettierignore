node_modules
dist
build
coverage
.next
.turbo
.venv
playwright-report
test-results
pnpm-lock.yaml
uv.lock
*.min.js
*.tsbuildinfo
apps/api/prisma/migrations
docs/PLAN.md
docs/CONTRACTS.md
docs/THREAT-MODEL.md

# Generated by `pnpm gen:client` with `JSON.stringify(document, null, 2)`, which
# disagrees with prettier about when an array fits on one line. The generator is
# the source of truth — formatting the file only means the next regeneration
# undoes it — so it is ignored rather than reformatted. Added by A25.
packages/api-client/openapi.json

# Helm templates are Go templates, not YAML: prettier cannot parse `{{ ... }}`
# where a key or a list item is expected. Added by X05.
infra/k8s/montaj/templates

# Generated render fixtures (A16): the golden command lists are written one style
# per line on purpose — a diff then names exactly which styles moved — and the
# PNG baselines are binary.
packages/render-core/fixtures/goldens
packages/render-canvaskit/fixtures/baselines
packages/render-canvaskit/e2e/.artifacts

# Playwright global-setup output (A18b), bundled fresh on every run: same reason
# as render-canvaskit's e2e/.artifacts above.
packages/fonts/e2e/.artifacts

# Generated by `scripts/build-pack.ts` with `JSON.stringify(manifest, null, 2)`,
# which disagrees with prettier about array wrapping — the same reason
# `packages/api-client/openapi.json` is ignored above. The build script is the
# source of truth; formatting the file only means the next `pack:build` undoes
# it. Added by A18b.
packages/fonts/pack/fonts.json
