node_modules
dist
build
coverage
.next
.turbo
.venv
playwright-report
test-results
pnpm-lock.yaml
uv.lock
*.min.js
*.tsbuildinfo
apps/api/prisma/migrations
docs/PLAN.md
docs/CONTRACTS.md
docs/THREAT-MODEL.md

# Generated by `pnpm gen:client` from the API's Swagger output and overwritten
# on every run; formatting it would be undone by the next generation and shows up
# as noise in the diff. Added by A13 (it already failed `format:check`).
packages/api-client/openapi.json
packages/api-client/src/generated

# Helm templates are Go templates, not YAML: prettier cannot parse `{{ ... }}`
# where a key or a list item is expected. Added by X05.
infra/k8s/montaj/templates
